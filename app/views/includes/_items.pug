section
  .items-wrapper
    ul.items-photo#items
      each items in item
        li
          a(href="/items/"+ items._id)
            img(src= 'images/' + items.url_images[0])
              //- "images/282_1.290x484.jpg"
            .items-text-block
              h2 #{items.name_dress}
              p артикул:
                span #{items.article}
              p цена:
                span #{items.price}грн
              button(type="submit", href="/items/"+ items._id).items-button-buy Описание
